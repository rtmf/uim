#!/bin/sh
set -e

if [ "$1" = configure ]; then
	update-alternatives --install /usr/bin/uim-toolbar uim-toolbar \
		/usr/bin/uim-toolbar-gtk-systray 70
	update-alternatives --install /usr/bin/uim-toolbar uim-toolbar \
		/usr/bin/uim-toolbar-gtk 60
fi

#DEBHELPER#

exit 0
